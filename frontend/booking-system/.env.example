# ============================================
# BOOKING SYSTEM - Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values
# cp .env.example .env.local

# Convex Deployment (auto-configured during dev:setup)
CONVEX_DEPLOYMENT=dev:your-deployment-id
CONVEX_URL=https://your-deployment-id.convex.cloud

# Optional: Resend API Key for sending real verification emails
# Without this, codes will be displayed in the UI (demo mode)
# RESEND_API_KEY=re_your_api_key_here
